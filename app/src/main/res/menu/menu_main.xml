<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity"
    >

    <item
        android:id="@+id/main_action_notifications"
        android:title="@string/menu_enable_notifications"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/main_action_info"
        android:title="@string/menu_info"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/main_action_carrier"
        android:title="@string/menu_carrier"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/main_action_filter"
        android:title="@string/menu_filter"
        app:showAsAction="always"
        />

</menu>
